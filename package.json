{
  "name": "tab-groups-windows-list",
  "version": "1.2.0",
  "description": "A simple expandable list of current tab groups and their window names.",
  "main": "popup.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:all": "npm run test && npm run test:e2e"
  },
  "keywords": [
    "chrome",
    "extension",
    "tab-groups",
    "windows"
  ],
  "author": "",
  "license": "Unlicense",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "jest": "^30.2.0",
    "playwright": "^1.57.0"
  }
}
